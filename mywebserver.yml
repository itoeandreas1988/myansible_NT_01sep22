---
- hosts: all
  become: yes
  tasks:
  - name: I want to install apache2 webserver on ubuntu node
    apt:
      name: apache2
      state: present

  handlers:
    - name: restart-apache
      service:
        name: apache2
        state: restarted
